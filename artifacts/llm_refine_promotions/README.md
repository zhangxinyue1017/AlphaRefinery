# llm_refine_promotions

这里放的是 `llm_refine` 从 research 结果走向正式代码沉淀时的中间层产物。

当前主要包括：

- `pending/`
  - pending curated manifest
  - preview py
  - pending patch
  - auto-apply 记录

一句话理解：

- 这里不是最终 `run`
- 也不是最终 `factors/llm_refined/*.py`
- 而是 promotion 过渡层
