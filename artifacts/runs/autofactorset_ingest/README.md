# autofactorset_ingest

这里是新的 `evaluate_registry_manifest.py` 默认 run 根目录。

放在这里的是 bridge 评测与 admission 运行结果，例如：

- `summary.csv`
- `results.json`
- 每个因子的 backtest report 目录

历史结果仍然可能保留在：
- `/root/workspace/zxy_workspace/AlphaRefinery/artifacts/autofactorset_ingest/runs/`

而 admission 的配置和清单仍然留在：
- `/root/workspace/zxy_workspace/AlphaRefinery/artifacts/autofactorset_ingest/manifests/`
- `/root/workspace/zxy_workspace/AlphaRefinery/artifacts/autofactorset_ingest/validated/`
